<Window x:Class="KonturEdoClient.CounteragentConsigneesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KonturEdoClient"
        mc:Ignorable="d"
        Title="Список грузополучателей" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <dxg:GridControl ItemsSource="{Binding ItemsList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                         SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0">
            <dxg:GridColumn FieldName="ConsigneeId" Header="ID" Width="80"/>
            <dxg:GridColumn FieldName="ConsigneeName" Header="Наименование" Width="120"/>
            <dxg:GridColumn FieldName="ConsigneeAddress" Header="Адрес" Width="*"/>
        </dxg:GridControl>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="5" Width="80" Content="Добавить" Command="{Binding CreateNewCommand}" />
            <Button Margin="5" Width="80" Content="Удалить" Command="{Binding DeleteCommand}" />
            <Button x:Name="CancelButton" Margin="5" Width="80" Content="Отмена" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
